import {describe,expect,it,vi} from 'vitest';import {loadTechnicalDrawings,loadTechnicalDrawingsForProduct} from './technicalDrawingsGateway'
describe('technical drawings gateway',()=>{it('reads global drawings',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true,desenhos:[]}),{status:200})));await loadTechnicalDrawings();expect(fetch).toHaveBeenCalledWith('/api/engenharia/desenho-tecnico/todos',expect.objectContaining({credentials:'include'}))});it('encodes product lookup',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true,desenhos:[]}),{status:200})));await loadTechnicalDrawingsForProduct('A/B');expect(fetch).toHaveBeenCalledWith('/api/engenharia/desenho-tecnico/A%2FB',expect.anything())})})
