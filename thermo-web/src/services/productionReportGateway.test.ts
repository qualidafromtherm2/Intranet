import {describe,expect,it,vi} from 'vitest';import {loadProductionReport} from './productionReportGateway';describe('productionReportGateway',()=>{it('uses authenticated GET contract',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,kpis:{}})});vi.stubGlobal('fetch',f);await loadProductionReport('trimestre');expect(f).toHaveBeenCalledWith('/api/producao/relatorio-gerencial?modo=trimestre',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
