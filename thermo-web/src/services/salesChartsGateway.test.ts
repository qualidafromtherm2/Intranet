import { describe, expect, it, vi } from 'vitest'
import { loadSalesFamilyDetail, loadSalesQuantityByFamilyMonth } from './salesChartsGateway'
describe('salesChartsGateway',()=>{it('uses read-only authenticated chart contracts',async()=>{const fetchMock=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,rows:[]})});vi.stubGlobal('fetch',fetchMock);await loadSalesQuantityByFamilyMonth();expect(fetchMock).toHaveBeenCalledWith('/api/sac/vendas/graficos/quantidade-familia-mes',expect.objectContaining({credentials:'include',cache:'no-store'}))});it('encodes drill-down filters',async()=>{const fetchMock=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,rows:[]})});vi.stubGlobal('fetch',fetchMock);await loadSalesFamilyDetail('Família A','2026-08');expect(fetchMock.mock.calls[0][0]).toContain('familia=Fam%C3%ADlia%20A&mes=2026-08')})})
