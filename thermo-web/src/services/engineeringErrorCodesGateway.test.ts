import {describe,expect,it,vi} from 'vitest';import {loadEngineeringErrorCodes} from './engineeringErrorCodesGateway';describe('engineeringErrorCodesGateway',()=>{it('encodes search and keeps GET-only contract',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,items:[]})});vi.stubGlobal('fetch',f);await loadEngineeringErrorCodes('E 01');expect(f).toHaveBeenCalledWith('/api/engenharia/codigos-erro?codigo=E%2001',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
