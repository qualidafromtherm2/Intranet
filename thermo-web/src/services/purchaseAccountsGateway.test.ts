import {describe,expect,it,vi} from 'vitest';import {loadPurchaseAccounts} from './purchaseAccountsGateway';describe('purchaseAccountsGateway',()=>{it('loads real accounts with GET-only request',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,contas:[]})});vi.stubGlobal('fetch',f);await loadPurchaseAccounts();expect(f).toHaveBeenCalledWith('/api/compras/contas-utilizadas',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
