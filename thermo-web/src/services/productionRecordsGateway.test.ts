import { describe,expect,it,vi } from 'vitest'
import { loadProductionRecordDetail,searchProductionRecords } from './productionRecordsGateway'
describe('production records gateway',()=>{it('uses readonly list contract',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({success:true,itens:[]}),{status:200})));await searchProductionRecords('OP 10');expect(fetch).toHaveBeenCalledWith('/api/producao/registro-producao?limit=50&q=OP+10',expect.objectContaining({credentials:'include'}))});it('requires id or OP only through query',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({success:true,maquina:{},postos:[],colaboradores:[]}),{status:200})));await loadProductionRecordDetail(7);expect(fetch).toHaveBeenCalledWith('/api/producao/registro-producao/detalhe?id=7',expect.anything())})})
