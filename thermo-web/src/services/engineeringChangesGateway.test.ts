import {describe,expect,it,vi} from 'vitest';import {loadEngineeringChanges,loadEngineeringChangesForProduct} from './engineeringChangesGateway'
describe('engineering changes gateway',()=>{it('reads global history contract',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true,alteracoes:[]}),{status:200})));await loadEngineeringChanges();expect(fetch).toHaveBeenCalledWith('/api/engenharia/alteracoes-produto/todos',expect.objectContaining({credentials:'include'}))});it('encodes product code',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true,alteracoes:[]}),{status:200})));await loadEngineeringChangesForProduct('AB/1');expect(fetch).toHaveBeenCalledWith('/api/engenharia/alteracoes-produto/AB%2F1',expect.anything())})})
