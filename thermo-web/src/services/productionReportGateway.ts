import type {ProductionReport} from '../features/production-report/types';export const loadProductionReport=async(modo='mes')=>{const r=await fetch(`/api/producao/relatorio-gerencial?modo=${encodeURIComponent(modo)}`,{credentials:'include',cache:'no-store',headers:{Accept:'application/json'}});const p=await r.json().catch(()=>({}));if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);return p as ProductionReport}
