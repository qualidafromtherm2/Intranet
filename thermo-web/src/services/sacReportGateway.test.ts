import {describe,expect,it,vi} from 'vitest';import {loadSacReport} from './sacReportGateway';describe('sacReportGateway',()=>{it('preserves GET report query',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,rows:[]})});vi.stubGlobal('fetch',f);await loadSacReport({periodo:'4'});expect(f).toHaveBeenCalledWith('/api/sac/at/relatorio-gerencial?periodo=4',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
