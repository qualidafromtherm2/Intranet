import type {SacReport} from '../features/sac-report/types';export const loadSacReport=async(filters:Record<string,string>={})=>{const q=new URLSearchParams(filters);const r=await fetch(`/api/sac/at/relatorio-gerencial?${q}`,{credentials:'include',cache:'no-store',headers:{Accept:'application/json'}});const p=await r.json().catch(()=>({}));if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);return p as SacReport}
