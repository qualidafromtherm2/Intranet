import type {GembaItem} from '../features/production-gemba/types';export const loadProductionGemba=async()=>{const r=await fetch('/api/producao/gemba',{credentials:'include',cache:'no-store',headers:{Accept:'application/json'}});const p=await r.json().catch(()=>({}));if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);return {items:(p.items||p.gembas||p.ocorrencias||[]) as GembaItem[]}}
