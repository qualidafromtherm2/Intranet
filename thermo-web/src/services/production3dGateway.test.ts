import {describe,expect,it,vi} from 'vitest';import {loadProduction3d} from './production3dGateway';describe('production3dGateway',()=>{it('loads the real endpoint as GET-only',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,total:0,itens:[]})});vi.stubGlobal('fetch',f);await loadProduction3d();expect(f).toHaveBeenCalledWith('/api/producao/cena-3d',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
