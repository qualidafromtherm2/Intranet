import {describe,expect,it,vi} from 'vitest';import {loadProductionGemba} from './productionGembaGateway';describe('productionGembaGateway',()=>{it('uses authenticated GET-only endpoint',async()=>{const f=vi.fn().mockResolvedValue({ok:true,json:async()=>({ok:true,items:[]})});vi.stubGlobal('fetch',f);await loadProductionGemba();expect(f).toHaveBeenCalledWith('/api/producao/gemba',expect.objectContaining({credentials:'include',cache:'no-store'}));expect(f.mock.calls[0][1].method).toBeUndefined()})})
