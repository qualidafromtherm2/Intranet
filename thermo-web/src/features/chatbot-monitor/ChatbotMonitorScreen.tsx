import {RefreshCw,Search,ShieldAlert} from 'lucide-react';import {useEffect,useState} from 'react';import {loadChatbotMonitor,loadChatbotMonitorDetails} from '../../services/chatbotMonitorGateway';import type {ChatbotMonitor,MonitorDetails,MonitorRow} from './types'
const label=(key:string)=>key.replaceAll('_',' ').replace(/([a-z])([A-Z])/g,'$1 $2')
const value=(v:unknown)=>v==null||v===''?'—':typeof v==='number'?v.toLocaleString('pt-BR'):String(v)
export function ChatbotMonitorScreen(){const[data,setData]=useState<ChatbotMonitor>();const[details,setDetails]=useState<MonitorDetails>();const[dataset,setDataset]=useState('faq');const[search,setSearch]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const load=async()=>{setLoading(true);try{setData(await loadChatbotMonitor());setDetails(await loadChatbotMonitorDetails({dataset,search,page:1,pageSize:20}));setError('')}catch(e){setError(e instanceof Error?e.message:'Falha ao carregar monitoramento.')}finally{setLoading(false)}};useEffect(()=>{void load()},[dataset]);return <main className="mx-auto max-w-[1400px] space-y-4 p-3 sm:p-6"><header><p className="text-xs font-bold uppercase tracking-wide text-violet-700">IA / Operação</p><h1 className="text-2xl font-bold text-thermo-navy">Monitor do chatbot</h1><p className="text-sm text-slate-600">Status de conhecimento, filas de aprendizagem e eventos recentes. Consulta somente leitura.</p></header>{error&&<p role="alert" className="rounded-md border border-red-200 bg-red-50 p-3 text-red-700"><ShieldAlert className="mr-2 inline size-4"/>{error}</p>}<section className="flex flex-wrap items-end gap-3 rounded-lg border border-thermo-border bg-white p-4"><label className="min-w-52 text-sm font-semibold">Explorar dataset<select className="mt-1 block min-h-10 w-full rounded-md border px-3" value={dataset} onChange={e=>setDataset(e.target.value)}><option value="faq">FAQs aprovadas</option><option value="messages">Mensagens</option><option value="memory">Memória</option></select></label><label className="min-w-60 text-sm font-semibold">Filtro de busca<div className="mt-1 flex min-h-10 items-center rounded-md border px-2"><Search className="size-4 text-slate-400"/><input className="w-full px-2 outline-none" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Pesquisar registros"/></div></label><button className="thermo-button thermo-button-secondary min-h-10" onClick={()=>void load()} disabled={loading} type="button"><RefreshCw className="size-4"/>Atualizar</button></section><section className="grid grid-cols-2 gap-3 sm:grid-cols-4">{Object.entries(data?.summary||{}).map(([k,v])=><article className="rounded-lg border border-thermo-border bg-white p-4" key={k}><span className="text-xs uppercase text-slate-500">{label(k)}</span><strong className="mt-1 block text-xl text-thermo-navy">{value(v)}</strong></article>)}</section><section className="grid gap-4 lg:grid-cols-3">{[['Perguntas sem resposta',data?.unresolvedQuestions],['Erros recentes',data?.recentErrors],['Distribuição do conhecimento',data?.learningReport?.faqPorArea]].map(([title,rows])=><article className="rounded-lg border border-thermo-border bg-white p-4" key={String(title)}><h2 className="mb-3 font-semibold text-thermo-navy">{title}</h2><ul className="max-h-56 space-y-2 overflow-auto text-sm">{(rows as MonitorRow[]|undefined)?.slice(0,8).map((row,i)=><li className="border-t pt-2" key={i}>{Object.entries(row).slice(0,3).map(([k,v])=><span className="mr-2" key={k}><b>{label(k)}:</b> {value(v)}</span>)}</li>)}{!rows?.length&&<li className="text-slate-500">Nenhum evento no período.</li>}</ul></article>)}</section><section className="overflow-x-auto rounded-lg border border-thermo-border bg-white p-4"><div className="mb-3 flex items-center justify-between"><h2 className="font-semibold text-thermo-navy">Detalhes: {dataset}</h2><span className="text-xs text-slate-500">{value(details?.total)} registros</span></div><table className="w-full min-w-[720px] text-left text-sm"><thead><tr>{Object.keys(details?.rows?.[0]||{}).map(k=><th className="border-b p-2 text-xs uppercase text-slate-500" key={k}>{label(k)}</th>)}</tr></thead><tbody>{details?.rows?.map((row,i)=><tr className="border-t" key={i}>{Object.entries(row).map(([k,v])=><td className="p-2" key={k}>{value(v)}</td>)}</tr>)}</tbody></table>{!details?.rows?.length&&<p className="p-8 text-center text-sm text-slate-500">Nenhum registro encontrado.</p>}</section></main>}
